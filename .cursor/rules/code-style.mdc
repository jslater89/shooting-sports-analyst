---
alwaysApply: true
---

# Dart Format
DO NOT RUN `dart format` unless requested.

# Responses
The chat window does not render MathJAX/MathML. Please write out math in code blocks.

# If style
Prefer:

```
if {

}
else {

}
```

over `... } else {`

# Loggers
Import lib/util with a package-relative path.

`final _log = SSALogger("<log-tag>");`

# List copying
Prefer the spread operator `var copy = [...oldList]`;